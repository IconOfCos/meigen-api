// APIルート定義のエントリーポイント
// 将来的に以下のルートを実装予定:
// - GET /quotes - 全ての名言を取得（ページネーション対応）
// - GET /quotes/random - ランダムな名言を1つ取得
// - GET /quotes/:id - 特定のIDの名言を取得
// - GET /quotes/category/:category - カテゴリ別名言取得
// - GET /quotes/author/:author - 作者別名言取得

// 現在は空のエクスポート - 実装時に各ルートを追加
export {};